WORKSPACE="{{ playbook_dir }}/.."
KAFL_ROOT="{{ install_root }}"
QEMU_ROOT="{{ repo.qemu.root }}"
LIBXDC_ROOT="{{ repo.libxdc.root }}"
CAPSTONE_ROOT="{{ repo.capstone.root }}"
RADAMSA_ROOT="{{ repo.radamsa.root }}"
KAFL_CONFIG_FILE="{{ install_root }}/kafl.yaml"
KAFL_WORKDIR="/dev/shm/{{ ansible_user_id }}_tdfl"
